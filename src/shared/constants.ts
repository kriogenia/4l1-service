/**
 * Environments of the API
 */
export enum Environment {
	AZ = "azure",
	DEV = "development",
	PROD = "production",
	TEST = "test"
}

/**
 * Severity levels
 */
 export enum Severity {
    INFO,
    IMP,
    WARN,
    ERR
}